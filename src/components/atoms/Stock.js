import React from 'react'
import {Fab} from '@mui/material';

export const Stock = ({labelAdd,labelRemove}) => {
    return (
        <div>
            {/* <Fab aria-label={label} className={className} color={color}>
                {icon}
            </Fab> */}
        </div>
    )
}

